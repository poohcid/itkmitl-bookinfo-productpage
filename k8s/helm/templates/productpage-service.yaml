apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.productpage.namespace }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.productpage.port }}
  {{- if .Values.productpage.targetPort }}
    targetPort: {{ .Values.productpage.targetPort }}
  {{- end }}
  selector:
    app: {{ .Release.Name }}